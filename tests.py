import unittest
import inspect
import sys
import InterfaceTest

class CheckInput(unittest.TestCase):

    # Check the input can be changed into an integer (is a single number)
    def test_integer(self):
        self
